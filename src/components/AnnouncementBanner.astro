---
import '../styles/announcement.css';
const announcements = [
    "🎉 Student Discount: 10% off Monday-Thursday",
    "💈 First Time Customer: €5 off your first haircut",
    "👥 Bring a friend and get 20% off both cuts",
    "🎫 Loyalty Card: Every 6th cut free"
];
---

<div class="bg-secondary text-black relative overflow-hidden h-10 will-change-transform">
    <div class="announcement-track whitespace-nowrap flex items-center h-full">
        <div class="flex items-center animate-scroll">
            {announcements.map((text) => (
                <span class="inline-block px-4 font-semibold text-lg tracking-wide">{text}</span>
            ))}
            {/* Duplicate announcements for seamless loop */}
            {announcements.map((text) => (
                <span class="inline-block px-4 font-semibold text-lg tracking-wide">{text}</span>
            ))}
        </div>
    </div>
</div>

<script>
    import { initAnnouncementBanner } from '../scripts/announcementBanner.js';
</script>